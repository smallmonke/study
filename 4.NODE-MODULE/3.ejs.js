//new Function + with来实现的
//const ejs = require('ejs');

const util = require('utl');

let ejs = {
    renderFile(filename,option){
        let content = await read(filename,'utf8');
    }
}
//返回promise
let r = await ejs.renderFile()